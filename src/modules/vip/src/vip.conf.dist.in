#####################################
# VIP Module Configuration          #
#####################################

[VipConf]
ConfVersion=2

###################################################################################################################
# VIP MODULE CONFIGURATION
#
#    Vip.Enable
#        Enable the VIP module + .vip grant/revoke chat commands + the master-spell cascade.
#        Default: 0 (off)
#                 1 (on)
#
#    Vip.MasterSpellId
#        ID of the spell taught to VIPs whose cast triggers the bundled
#        cascade. Defaults to vanilla spell 18282 "Dummy Spell" — already
#        SPELL_EFFECT_DUMMY in spell_template, exists in the client's
#        Spell.dbc (so it can be cast and displayed), has zero references
#        in item/trainer/script tables (so overriding via OnCast is safe).
#        No spell_template change required — the vanilla row works as-is.
#        Note: 91200 (a custom row) was tried first but vanilla 1.12
#        clients don't know that ID, so it couldn't be cast.
#        Default: 18282
#
#    Vip.BundledSpellIds
#        Comma-separated list of spell IDs the master cascades into when cast.
#        Each is cast as a triggered spell on the caster (bypasses GCD / mana
#        cost / cast time) and retains its original duration. Empty / unset
#        uses the built-in defaults:
#
#          22888  Rallying Cry of the Dragonslayer (Onyxia/Nef head buff)
#          24425  Spirit of Zandalar
#          23735  Mol'dar's Moxie       (DM tribute)
#          23736  Fengus' Ferocity      (DM tribute)
#          23737  Slip'kik's Savvy      (DM tribute)
#          15366  Songflower Serenade
#          16609  Warchief's Blessing
#          17626  Greater Arcane Elixir
#          17627  Distilled Wisdom flask
#          17628  Supreme Power flask
#
#        Override here to extend / trim. Whitespace ignored.
#        Default: "" (use built-in list)
#
###################################################################################################################

Vip.Enable = 1
Vip.MasterSpellId = 18282
Vip.BundledSpellIds = ""
